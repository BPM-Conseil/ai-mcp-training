POSTGRES_USER=mcp
POSTGRES_PASSWORD=mcppassword
POSTGRES_DB=mcpdb

# Embeddings used in MCP (OpenAI)
OPENAI_API_KEY=
EMBEDDING_MODEL_NAME=text-embedding-3-small

# Backend LLM (optional, if you also want generated answers)
OPENAI_MODEL=gpt-4o-mini

# Backend->MCP URL (compose internal)
MCP_WS_URL=ws://mcp-server:8765
